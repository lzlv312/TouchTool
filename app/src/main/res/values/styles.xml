<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.TouchTool.IconButton" parent="Widget.Material3.Button.IconButton">
        <item name="android:layout_width">?attr/iconButtonSize</item>
        <item name="android:layout_height">?attr/iconButtonSize</item>
        <item name="android:padding">?attr/iconButtonPadding</item>
    </style>

    <style name="Theme.TouchTool.IconButton.Outlined" parent="Widget.Material3.Button.IconButton.Outlined">
        <item name="android:layout_width">?attr/iconButtonSize</item>
        <item name="android:layout_height">?attr/iconButtonSize</item>
        <item name="android:padding">?attr/iconButtonPadding</item>
    </style>

    <style name="Theme.TouchTool.IconButton.Filled" parent="Widget.Material3.Button.IconButton.Filled">
        <item name="android:layout_width">?attr/iconButtonSize</item>
        <item name="android:layout_height">?attr/iconButtonSize</item>
        <item name="android:padding">?attr/iconButtonPadding</item>
    </style>

    <style name="Theme.TouchTool.IconButton.Tonal" parent="Widget.Material3.Button.IconButton.Filled.Tonal">
        <item name="android:layout_width">?attr/iconButtonSize</item>
        <item name="android:layout_height">?attr/iconButtonSize</item>
        <item name="android:padding">?attr/iconButtonPadding</item>
    </style>

</resources>